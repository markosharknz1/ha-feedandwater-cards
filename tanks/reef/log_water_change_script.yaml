script:
  reef_log_water_change:
    alias: "Main Display Tank - Log Water Change Now"
    sequence:
      - action: input_datetime.set_datetime
        target:
          entity_id: input_datetime.reef_last_water_change
        data:
          datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
    mode: single
