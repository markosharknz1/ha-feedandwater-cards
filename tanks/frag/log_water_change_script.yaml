script:
  frag_log_water_change:
    alias: "Frag Tank - Log Water Change Now"
    sequence:
      - action: input_datetime.set_datetime
        target:
          entity_id: input_datetime.frag_last_water_change
        data:
          datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
    mode: single
